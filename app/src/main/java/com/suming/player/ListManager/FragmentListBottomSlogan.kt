package com.suming.player.ListManager

import android.view.ViewGroup
import androidx.paging.LoadState
import androidx.paging.LoadStateAdapter

/*
class FragmentListBottomSlogan(
    private val retry: () -> Unit
) : LoadStateAdapter<FragmentPlayListBottonSloganHolder>() {

    override fun onCreateViewHolder(
        parent: ViewGroup,
        loadState: LoadState
    ) = FragmentPlayListBottonSloganHolder(parent, retry)



    override fun onBindViewHolder(
        holder: FragmentPlayListBottonSloganHolder,
        loadState: LoadState
    ) {
        holder.bind(loadState)
    }
}

 */