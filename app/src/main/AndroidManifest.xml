<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="ImpliedTouchscreenHardware,MissingLeanbackLauncher,MissingLeanbackSupport">


    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:ignore="PhotoAndVideoPolicy" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:ignore="PhotoAndVideoPolicy" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- 12及以下 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="AllFilesAccessPolicy,ScopedStorage" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.REORDER_TASKS"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.DOWNLOAD_FILES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />



    <!-- 废弃权限
    //1.QUERY_ALL_PACKAGES 会被拒绝上架,必须在queries中指定要查询的包名
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore=",PackageVisibilityPolicy" />
     -->

    <queries>
        <package android:name="com.google.android.gms" /> <!-- MicroG (跟gms一样的) -->

    </queries>


    <application
        android:name=".Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.SpeedSpy"
        tools:replace="android:allowBackup"
        android:enableOnBackInvokedCallback="false"
        tools:ignore="UnusedAttribute">



        <!-- 主页 -->
        <activity android:name=".MainActivity"
            android:theme="@style/THEME_BASIC"
            android:hardwareAccelerated="true"
            android:label="播放器"
            android:launchMode="singleTop"
            android:exported="true"
            android:enableOnBackInvokedCallback="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.notch_support"
                android:value="true"/>
        </activity>

        <!-- 隐私权限面板 -->
        <activity android:name=".PrivacyPermissionActivity"
            android:theme="@style/THEME_BASIC"
            android:hardwareAccelerated="true"
            android:label="隐私和权限"
            android:exported="true"
            android:launchMode="singleTop"
            android:enableOnBackInvokedCallback="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <meta-data
                android:name="android.notch_support"
                android:value="true"/>
        </activity>

        <!-- 播放页Neo -->
        <activity android:name=".PlayerActivityNeo"
            android:theme="@style/THEME_Player"
            android:label="播放器Neo"
            android:screenOrientation="unspecified"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|screenSize|screenLayout"
            android:launchMode="singleTask"
            android:exported="true"
            android:enableOnBackInvokedCallback="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <meta-data
                android:name="android.notch_support"
                android:value="true"
                />
        </activity>


        <!-- 播放页Simple -->
        <activity android:name=".PlayerActivitySimple"
            android:theme="@style/THEME_Player"
            android:label="播放器Simple"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|screenLayout"
            android:launchMode="singleTask"
            android:enableOnBackInvokedCallback="false"
            android:exported="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <meta-data
                android:name="android.notch_support"
                android:value="true"/>
        </activity>


        <!-- EntranceActivity -->
        <activity android:name=".EntranceActivity"
            android:theme="@style/THEME_Player"
            android:label="播放器"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|screenLayout"
            android:launchMode="singleTask"
            android:enableOnBackInvokedCallback="false"
            android:exported="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <meta-data
                android:name="android.notch_support"
                android:value="true"/>
        </activity>


        <!-- 设置 -->
        <activity android:name=".SettingsActivity"
            android:theme="@style/THEME_FULL_SCREEN"
            android:screenOrientation="unspecified"
            android:label="设置"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity"/>
        <!-- 指南和帮助 -->
        <activity android:name=".GuidanceActivity"
            android:theme="@style/THEME_BASIC"
            android:screenOrientation="unspecified"
            android:label="指南和帮助"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity"/>
        <!-- 设备信息 -->
        <activity android:name=".DeviceInfoActivity"
            android:theme="@style/THEME_BASIC"
            android:screenOrientation="unspecified"
            android:label="设备信息"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            tools:ignore="DiscouragedApi,LockedOrientationActivity"/>


        <!-- 后台播放服务 -->
        <service android:name=".ActivityComponent.PlayerService.PlayerService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService"/>
            </intent-filter>
        </service>
        <!-- 悬浮窗服务 -->
        <service android:name=".IndepService.FloatingWindowService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse" />
        <!-- File Provider -->
        <provider android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- 播放控制广播接收器 -->
        <!--
        <receiver android:name=".PlayerActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="PLAYER_PLAY"/>
                <action android:name="PLAYER_PAUSE"/>
            </intent-filter>
        </receiver>
         -->
        <!-- 媒体会话操作接收器(没用) -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>


    </application>

</manifest>